import React,{useState} from 'react';
import {FlatList,Modal,Text,TouchableOpacity,View} from 'react-native';
import {colors,common} from '../theme';
type Props={label:string;value:string;options:{label:string,value:string}[];onChange:(v:string)=>void;placeholder?:string};
export default function SelectField({label,value,options,onChange,placeholder='Seleccionar'}:Props){const [open,setOpen]=useState(false);const selected=options.find(o=>o.value===value);return <View><Text style={common.label}>{label}</Text><TouchableOpacity onPress={()=>setOpen(true)} style={[common.input,{justifyContent:'center'}]}><Text style={{color:selected?colors.text:colors.muted}}>{selected?.label||placeholder}</Text></TouchableOpacity><Modal visible={open} transparent animationType="slide" onRequestClose={()=>setOpen(false)}><View style={{flex:1,backgroundColor:'rgba(0,0,0,.35)',justifyContent:'flex-end'}}><View style={{backgroundColor:'#fff',borderTopLeftRadius:20,borderTopRightRadius:20,maxHeight:'70%',padding:20}}><Text style={[common.title,{fontSize:20}]}>Seleccionar {label.toLowerCase()}</Text><FlatList data={options} keyExtractor={i=>i.value} renderItem={({item})=><TouchableOpacity onPress={()=>{onChange(item.value);setOpen(false)}} style={{padding:15,borderBottomWidth:1,borderBottomColor:colors.border}}><Text style={{fontSize:16,fontWeight:item.value===value?'800':'500'}}>{item.label}</Text></TouchableOpacity>}/><TouchableOpacity onPress={()=>setOpen(false)} style={{marginTop:12,alignItems:'center',padding:12}}><Text style={{color:colors.danger,fontWeight:'800'}}>CANCELAR</Text></TouchableOpacity></View></View></Modal></View>}
